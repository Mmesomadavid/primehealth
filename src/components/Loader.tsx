const Loader = () => {
  return (
    <div className="flex items-center justify-center h-screen bg-blue-700">
      <div className="loader"></div> {/* Use the global .loader class */}
    </div>
  );
};

export default Loader;
